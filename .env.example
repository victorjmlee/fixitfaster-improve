# Fix It Faster - Datadog 연동
# 이 파일을 복사해서 .env.local 로 저장한 뒤 값을 채우세요.

# 필수: Datadog API Key (Organization Settings > API Keys)
# - 앱 연결 테스트 + APM 트레이스 전송(agentless) 모두 사용
DATADOG_API_KEY=your_api_key_here

# 선택: 일부 기능용 Application Key (Organization Settings > Application Keys)
DATADOG_APP_KEY=your_app_key_here

# 선택: EU 등 다른 사이트 사용 시 (기본: datadoghq.com)
# DATADOG_SITE=datadoghq.eu

# --- APM: 각자 환경에서 Agent 띄워서 실습할 때 ---
# 1) docker compose up -d agent  (이 파일의 DATADOG_API_KEY 사용)
# 2) 아래 주석 해제 후 앱 재시작 → 트레이스가 로컬 Agent 경유로 본인 Datadog에 전송됨
# DD_AGENT_HOST=localhost
# DD_SERVICE=fixitfaster
# DD_ENV=development
