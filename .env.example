# --- LLM grading: use either AI Gateway (internal) or Gemini (external) ---

# AI Gateway (internal): preferred when running in staging/AppGate.
# Local/staging: https://ai-gateway.us1.staging.dog/v1
# Token: ddtool auth token rapid-ai-platform --datacenter us1.staging.dog
# For Evaluation account (PoC): AI_GATEWAY_EVAL=true
# AI_GATEWAY_BASE_URL=https://ai-gateway.us1.staging.dog/v1
# AI_GATEWAY_TOKEN=<from ddtool or Internal Service Auth>
# AI_GATEWAY_SOURCE=fixitfaster-leaderboard
# AI_GATEWAY_ORG_ID=2
# AI_GATEWAY_EVAL=true

# Gemini (external fallback): only used if AI Gateway is not configured.
# GEMINI_API_KEY=your_gemini_api_key_here
